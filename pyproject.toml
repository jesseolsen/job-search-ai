[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "job-search-ai"
version = "0.1.0"
description = "Learning portfolio: AI-powered job search tools with LangGraph, PyTorch, TensorFlow, and Claude API"
requires-python = ">=3.10"
